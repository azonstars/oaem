--- server.ts
+++ server.ts
@@ -2102,6 +2102,8 @@
       if (user.role === "Sub-office User" && ["Allocations", "Expenses", "NoteSheets"].includes(sheet)) {
         data = data.filter((item: any) => item.officeId === user.officeId);
+      } else if (user.role === "Sub-office User" && sheet === "Users") {
+        data = data.filter((item: any) => item.officeId === user.officeId);
       }
       if (sheet === "Users") {
         data = data.map((u: any) => {
